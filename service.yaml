apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-service
spec:
  type: LoadBalancer
  selector:
    app: aceest-fitness-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
